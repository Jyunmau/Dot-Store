# Sprint 3: 收支记录模块实施计划

## 一、后端开发

### 1. 数据模型层 (`apps/api-server/app/models/`)

* 创建 `transaction.py`：Transaction 模型（收支记录）和 TransactionCategory 模型（收支分类）

* 更新 `__init__.py`：导出新模型

### 2. Schema层 (`apps/api-server/app/schemas/`)

* 创建 `transaction.py`：定义请求/响应模型

  * TransactionCreate, TransactionUpdate, TransactionResponse

  * TransactionCategoryCreate, TransactionCategoryUpdate, TransactionCategoryResponse

  * TransactionFilters, TransactionListResponse, TransactionSummary

### 3. 服务层 (`apps/api-server/app/services/`)

* 创建 `transaction_service.py`：收支记录业务逻辑

* 创建 `transaction_category_service.py`：收支分类业务逻辑

### 4. API路由层 (`apps/api-server/app/api/v1/`)

* 创建 `transactions.py`：收支记录API（CRUD + 汇总统计）

* 创建 `transaction_categories.py`：收支分类API

* 创建 `upload.py`：文件上传API（凭证图片）

* 更新 `__init__.py`：注册新路由

### 5. 数据库迁移

* 创建Alembic迁移脚本

## 二、前端开发

### 1. 类型定义 (`apps/frontend/src/types/`)

* 创建 `transaction.ts`：Transaction、TransactionCategory等类型定义

### 2. 服务层 (`apps/frontend/src/services/`)

* 创建 `transactionService.ts`：收支API调用封装

### 3. 状态管理 (`apps/frontend/src/store/`)

* 创建 `transactionStore.ts`：收支状态管理

### 4. 页面组件 (`apps/frontend/src/pages/transaction/`)

* 创建 `TransactionList.tsx`：收支列表页面

* 创建 `TransactionForm.tsx`：收支表单组件

* 创建 `TransactionCategoryManage.tsx`：收支分类管理页面

* 创建 `index.ts`：导出组件

### 5. 路由配置

* 更新 `App.tsx`：添加收支相关路由

## 三、测试与验证

1. 启动后端服务，验证API接口
2. 启动前端服务，测试收支功能
3. 测试用例验证：

   * 收入记录创建、查看、编辑、删除

   * 支出记录创建、查看、编辑、删除

   * 收支分类管理

   * 凭证图片上传

   * 收支列表筛选

## 四、文档更新

* 更新 README.md：添加Sprint 3功能说明和API接口文档

## 五、提交代码

* 停止服务

* Git提交代码

